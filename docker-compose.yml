version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend:/usr/src/backend
  mongo:
    image: "mongo:latest"
    ports:
      - "27017:27017"
